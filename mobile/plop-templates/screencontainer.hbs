import React from 'react';

import {{name}} from './{{name}}';

function {{name}}Container(props) {
  const _props = {
  };

  const methods = {};

  return <{{name}} {...{ ..._props, ...methods }} />;
}

{{name}}Container.propTypes = {
  navigation: PropTypes.navigation,
};

export default {{name}}Container;
